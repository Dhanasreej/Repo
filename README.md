IP NOTICE:
Font needed to be changed from Segoe UI to Arial.
Height of the row 1 differs.
Extend the width of the cell B2 as the sentence is broken into three lines instead of two.
Apply thick full outside border of black color to the cell B2.
LATE APPEALS:
Hide row 1 and shrink the size of row 3.
Remove the wrap text applied to the subtitle.
The merged cells should be changed to B2-F2 instead of B2-E2.
Increase the height of Row2 for subtitle and Row4 for headers row height.
Records are in different order.
All the column headers are mid left aligned instead of bottom left aligned.
The size of the header text should be 9 instead of 10.
The wrap text should be applied only to the 2nd Appeal Determination Date column records hence remove the wrap text on all other column records.
Hide the column A and B.
The table should start from the cell C4 instead of B4.
Records of the column Claim detail key and all the Days Late columns should be bottom right aligned instead of middle right aligned.
The records of all other columns should be bottom left aligned instead of middle left aligned.
The data has a header style applied, but it is not fully formatted as an excel table. The rows do not have banded colors.
The header row should have border and the hex code for the color is #3877A6.
In the Appeal Received this Week (1,2,3) column header there is and extra white space before the open parentheses.
In the 1st Appeal Received Date column header there should be an extra space included at the end of the header text.
In the 1st Appeal Task Time Out Date column header there is an extra space after the open parentheses.
In the 2nd Appeal Task Time Out Date (Anchored on 2nd Determination Date) column header there is an extra space after the open parentheses and the 2st must be changed to 2nd.
In the Approve Late Appeal Yes or NO column header the Yes or NO should have a line break and be shifted to the next line.
Claim Detail Key column visually shows two decimal point after the Claim Detail Key hence that should be removed.
Appeal received this week (1,2,3) column values are same but datatype mismatch it must be in number as text type.
In the Appeal Received Date column the datatype is date time, it should date format and few values are missing. (for entered values the date matches)
In the Audit Determination Letter Date column, the datatype is date time instead of date.  (dates do not match for most of the entered values)
In the Days Late column few values are incorrect and few are missing.
In the 1st Appeal Determination Date column, the datatype is of date time instead of date. (but the date value matched)
In the 2nd Appeal Received Date column datatype is different and many values are missing.
In the Days Late column few values are incorrect and few are missing.
In the 2nd Appeal Determination Date column, the datatype is of date time instead of date. (but the date value matched)
In the Days Late column few values are incorrect and few are missing.
