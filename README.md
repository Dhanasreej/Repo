TAB : IP NOTICE :
•	Reduce the width of the box (column size) and increase the length of the box (row size)
•	The size of the column A is little large so reduce the size 
TAB : Details :
Data Validation :
•	In (1, 0)Invoiced field, the value got differed  for Cotiviti ID - 6653938456001
•	Total value for Estimated Savings got changed (last row)
•	1st three Cotiviti IDs are missing and respective values for those IDs in other fields also got lost in RDL but the total count matches and those three IDs are replaced by other three IDs in various locations and hence the table order varies 
•	Value in Paid Date field changes for Cotiviti ID - 7705332011001
Formatting :
•	Reduce the size of Column A
•	Reduce the size of Row 1
•	Increase the length of subtitle box(2nd row) and reduce the width 
•	Reduce the column size for all fields 
•	Increase the row size for all values including header and last total row
•	The colour code of the subtitle box differs
•	The values inside the table got wrapped including the last total row
